import SetupWrapper from "@/presentation/components/layout/SetupWrapper"

export default function SetupLayout({ children }: { children: React.ReactNode }) {
  return <SetupWrapper>{children}</SetupWrapper>
}
